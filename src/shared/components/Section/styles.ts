import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    marginVertical: 5,
  },
  contentContainer: {
    // Content container styles can be customized via props
  },
});

