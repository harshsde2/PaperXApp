import { StyleSheet } from 'react-native';

// ToastListener does not render visible UI but we keep
// a styles file for structural consistency.
export const styles = StyleSheet.create({
  hidden: {
    display: 'none',
  },
});

